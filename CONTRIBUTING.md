# Bordo 협업 규칙

> `frontend` · `backend` · `discord` 세 저장소 공통. 세 곳에 같은 파일이 들어 있습니다.
>
> 대회가 **깃 기록과 역할 분배를 세세히 평가**합니다. 이 규칙의 목적은 코드 정리가 아니라
> **"누가 무엇을 왜 했는지"가 기록만 봐도 드러나게 하는 것**입니다.

---

## 1. 커밋 메시지

```
<type>(<scope>): <제목>

<본문 — 왜 이렇게 했는지>

<푸터 — 이슈 참조 · 공동 작업자>
```

**타입과 스코프는 영어 소문자, 제목과 본문은 한국어.**

### 타입 6종

| 타입 | 쓸 때 | 예시 |
|---|---|---|
| `feat` | 사용자가 쓸 수 있는 기능이 늘어남 | `feat: 회의 불참 등록 UI 구현` |
| `fix` | 의도와 다르게 동작하던 것을 고침 | `fix: 플로우 필터가 카테고리 검증을 건너뛰는 문제 수정` |
| `docs` | 문서·주석·명세만 변경 | `docs: 2차 회의 결정 기록 추가` |
| `refactor` | 동작은 그대로, 구조만 변경 | `refactor: 홈 집계 쿼리를 단일 조회로 정리` |
| `chore` | 의존성·설정·빌드·잡일 | `chore: Discord 봇 의존성 버전 고정` |
| `design` | 디자인 토큰·에셋·화면 정의서 | `design: 대시보드 디자인 토큰 정의` |

- `design`은 표준 Conventional Commits에 없는 우리 추가분입니다. **디자이너 커밋 0건이 가장 큰 구멍**이라
  디자인 산출물이 기록에 남도록 별도 타입으로 뒀습니다.
- 어디에 넣을지 애매하면 **"사용자에게 보이는 변화가 있는가"**로 가릅니다. 있으면 `feat`/`fix`, 없으면 나머지.

### 스코프 (선택)

한 저장소 안에서 어느 영역인지 밝힐 때만 씁니다. 없어도 됩니다.

| 저장소 | 스코프 예 |
|---|---|
| backend | `agent` `meetings` `orgs` `home` `common` `config` |
| frontend | `home` `flow` `agent` `shared` `lib` |
| discord | `meeting` `outbox` `skill` `command` |

```
feat(meetings): 회의 시작·종료 내부 엔드포인트 추가
design(flow): 엣지 투명도 단계 토큰 정의
```

### 제목 규칙

- **50자 내외.** 넘으면 커밋을 쪼갤 때가 된 것입니다.
- 마침표 없음.
- 한국어 어미를 그대로 씁니다 — `~구현`, `~수정`, `~추가`, `~정리`.
- **무엇을 했는지가 아니라 무엇이 달라지는지**를 씁니다.

```
❌ fix: 버그 수정
❌ chore: 코드 정리
❌ feat: HomePage.jsx 수정
✅ fix: 회의 종료 후 브리핑이 두 번 생성되는 문제 수정
✅ feat: 홈 화면에 불참 회의 뱃지 노출
```

### 본문 (선택이지만 권장)

**무엇을 했는지는 diff에 있습니다. 본문에는 왜 그렇게 했는지를 씁니다.**
결정에 근거가 있으면 반드시 남깁니다 — 나중에 되돌릴지 판단하는 사람이 읽습니다.

```
fix: 플로우 엣지 투명도를 회의 구간 기준으로 계산

조회 시각을 기준으로 하면 같은 회의를 내일 열었을 때 그림이 달라진다.
회의 start~end 구간 안에서의 상대 위치로 바꿨다.
```

한 줄에 72자 정도에서 줄바꿈합니다.

### 푸터

```
Refs: #12                        관련 이슈
Closes: #12                      이 커밋으로 닫히는 이슈
Co-authored-by: 유수인 <email>    함께 작업한 사람
BREAKING CHANGE: <설명>          API 계약이 깨질 때
```

- **`Co-authored-by:`를 적극적으로 씁니다.** 페어로 작업하거나 화면 옆에서 같이 결정한 경우,
  기여자가 GitHub 기록에 함께 남습니다. 협업 증거로 직접 쓰입니다.
- `BREAKING CHANGE:`는 프론트 목 데이터 형태나 API 응답 스키마가 바뀔 때 반드시 붙입니다.
  **목 데이터 형태 = API 계약**이라 한쪽만 바꾸면 다른 쪽이 조용히 깨집니다.

### 커밋을 쪼개는 기준

한 커밋은 **되돌릴 때 통째로 되돌려도 말이 되는 단위**여야 합니다.

- 기능 추가와 리팩터링을 한 커밋에 섞지 않습니다.
- 포매팅 변경은 로직 변경과 분리합니다. 섞이면 리뷰어가 실제 변경을 찾지 못합니다.
- 반대로 **동작하지 않는 중간 상태를 커밋하지 않습니다.** 커밋 하나는 그 자체로 돌아가야 합니다.

---

## 2. 브랜치

```
main       시연 가능한 안정 버전. 직접 커밋 금지
 └─ develop   통합 브랜치. 여기로 PR을 엽니다
     └─ feat/12-flow-filter
     └─ fix/18-home-badge
     └─ design/21-dashboard-token
```

**이름: `<type>/<이슈번호>-<영문-요약>`** — 타입은 커밋 타입 6종과 같은 것을 씁니다.

```
feat/12-flow-filter
fix/18-briefing-duplicate
docs/24-meeting-decision
```

- `main` · `develop`에 **직접 푸시하지 않습니다.** force push는 어떤 경우에도 금지.
- 브랜치는 머지되면 지웁니다.
- 세 저장소 모두 `main` · `develop`이 준비돼 있습니다. PR은 `develop`으로 엽니다.

---

## 3. 이슈 → 브랜치 → PR

이 순서를 고정합니다. **역할 분배가 그 자체로 평가 자료**가 되기 때문입니다.

1. **이슈 생성** — 담당자·라벨 지정. 라벨은 저장소별 영역(`flow`, `agent`, `meeting`, `design` 등)
2. **브랜치 생성** — 이슈 번호를 이름에 넣습니다
3. **PR** — 제목은 커밋 메시지와 같은 형식. 본문에 `Closes: #12`

### PR 본문 템플릿

```markdown
## 무엇을
<한 줄>

## 왜
<배경. 회의 결정이면 해당 회의를 링크>

## 확인 방법
<리뷰어가 직접 돌려볼 수 있는 명령이나 화면 경로>

## 다른 저장소에 영향
<없음 / 있으면 어느 저장소의 무엇이 바뀌어야 하는지>

Closes: #12
```

- **"다른 저장소에 영향" 칸을 비워두지 않습니다.** 3개 저장소가 API 계약으로 묶여 있어,
  여기가 유일한 교차 알림 수단입니다.

### 리뷰

- **모든 PR은 담당 저장소 밖 1명이 승인합니다** (교차 리뷰). 리뷰 코멘트 자체가 협업 증거입니다.
- 승인 없이 머지하지 않습니다. 급하면 승인을 받고 머지합니다.
- 리뷰어는 "LGTM" 한 줄만 남기지 않습니다. 무엇을 확인했는지 한 문장이라도 적습니다.

---

## 4. 결정 기록

기획이 사실상 1인에게서 나오므로, 논의 과정이 남지 않으면 **협업이 아니라 지시로 보입니다.**

- 경로: `docs/decisions/YYYY-MM-DD-주제.md`
- 커밋: `docs: <주제> 결정 기록` — **논의를 주도한 사람 계정으로 커밋합니다**
- 대상: 회의 결정, 범위 변경, API 계약 변경, 미확정 항목이 확정될 때

```markdown
# 2026-08-14 메인 화면 중심 결정

## 논의한 것
A. 최근 회의·변경사항 중심 / B. AI 대화 중심

## 결정
A

## 근거
사용자가 서비스를 켜는 이유는 "AI와 얘기하고 싶어서"가 아니라
"내가 없는 동안 뭐가 있었지?"다. B는 "무엇을 물어볼지" 생각하는 부담을 추가로 지운다.

## 반대 의견
서재민 — 팀 진입 직후 대리인과 바로 대화하는 쪽이 접근 단계가 짧다

## 영향
frontend 전체 라우팅 구조, backend `GET /api/v1/home` 응답 범위
```

**반대 의견 칸을 지우지 않습니다.** 이견이 있었다는 기록이 논의가 실제로 있었다는 증거입니다.

---

## 5. 하지 않는 것

| 금지 | 이유 |
|---|---|
| `main` · `develop` 직접 푸시, force push | 되돌릴 수 없습니다 |
| `수정`, `ㅇㅇ`, `asdf`, `커밋` 같은 제목 | 기록이 평가 대상입니다 |
| 하나의 커밋에 여러 목적 | 리뷰도 롤백도 불가능해집니다 |
| 남의 영역을 효율을 위해 대신 구현 | 역할 분배가 평가 대상입니다 |
| 디자인 산출물을 개발자가 대신 커밋 | 디자이너 커밋 기록이 남지 않습니다 |
| 커밋 히스토리 rewrite (rebase 후 force) | 이미 공유된 기록이 사라집니다 |

---

## 6. 한눈에

```bash
# 1. 이슈 만들고 번호 확인 (#12)
# 2. 브랜치
git switch develop && git pull
git switch -c feat/12-flow-filter

# 3. 작업하고 커밋
git commit -m "feat(flow): 사람·회의·시간 3종 필터 구현

Flow 화면이 정보 과다로 읽히지 않는다는 2차 회의 지적에 따라
기본 상태에서 최근 항목만 보이도록 했다.

Refs: #12"

# 4. PR → develop, 저장소 밖 1명 리뷰 요청
git push -u origin feat/12-flow-filter
```
